<?PHP session_start();?>
<?

include 'process/config.php'

// Select statment for all students

if($_POST['email'] == $row['studentEmail'] && $_POST['password'] == $row['studentPassword']){

//once the person is selected set all variables equal to session variables and user the header function to send the user to the dashboard

}
?>
